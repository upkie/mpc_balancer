name: mpc_balancer
channels:
    - https://conda.anaconda.org/conda-forge
dependencies:
    - gin-config >= 0.5.0
    - numpy >= 1.24.3
    - pip
    - proxsuite >= 0.4.1
    - qpsolvers >= 4.3.3
    - upkie >= 5.0.1
    - pip:
        - qpmpc >= 3.0.0
